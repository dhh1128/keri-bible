# Dossier Doctrine Dossier (KSWG Dossier Specification + KERI/ACDC/CESR primer & refs)

**Pin: `trustoverip/kswg-dossier-specification` `main` at `b9227753c` (2026-08-25, "Correct signing language in the schemas and recompute their SAIDs"). Tier marker `[N]` — this is the line the community is standardizing. Mined 2026-09-15. Previous pin `3900e62` (2026-07-17).**

Sources:
- `spec/dossier-spec-body.md` — the normative Dossier spec body (649 lines at `b9227753c`)
- `spec/dossier-spec-head.md` — front matter: abstract, introduction, W3C VP comparison, scope (newly mined this pass; not previously a source of record)
- `spec/terms-definitions/*.md` — the glossary, one term per file
- `schemas/dossier-base.json`, `schemas/endorsement.json` — the two normative schemas
- `.ref/keri-primer.md` — "A Primer on KERI, ACDCs, and CESR" (19 Dec 2025) — **untracked, see the provenance warning below**
- `.ref/issuance-ref.md` — issuance/multisig/coordination technical summary — **untracked**
- `.ref/operators-ref.md` — ACDC Edge Operators reference — **untracked**

## 0. Provenance of this note, and two warnings about it

**Warning 1 — the previous pin `3900e62` was never on the standardizing line.** It resolves, and it is reachable: it is the tip of the branch `schema-base-dossier`, local and at `origin` (`git for-each-ref --contains 3900e62` returns only `refs/heads/schema-base-dossier` and `refs/remotes/origin/schema-base-dossier`). It is **not** an ancestor of `main` at `b9227753c`. So this note's entire joint-issuance section was mined from a feature branch that was ahead of `main`, and was marked `[N]` when the citation only supported something closer to `[N1.1]`. `main` at the time (`84e7b68`, the last commit before this window) still carried the older `M` / `RM` / `Q` / `FIN` operator family, with a numeric count in an `m` field and potential signers enumerated in an `mgrp` attribute — a model this note never described. **That tier overclaim has now resolved itself in the note's favour**: the branch's content landed on `main` through `4dae85a`..`ff5b0a4`, so the `MxN`/`RMxN`/`MxQ`/`RMxQ` family recorded below is genuinely `[N]` as of `b9227753c`. The lesson is worth keeping anyway — a hash that resolves is not evidence that a claim is normative; check what the hash is reachable *from*. `keri-doctrine.md` line 19 lists "Dossier spec `3900e62`" among its citation-durability pins, and carries the same defect.

**Warning 2 — the `.ref/` sources cannot be pinned to any commit, ever.** `.ref` is the last entry in `.gitignore` at `b9227753c`, and `git ls-tree -r b9227753c -- .ref` returns nothing. The three reference documents exist only as untracked files in Daniel's working copy. Sections 9, 10, 13 and 14 rest on them entirely, and part of section 7 does; sections 1-6, 8, 11, 12, 14A are spec-sourced and pinnable. Those claims therefore fail standards §1 ("at a **named commit**") not because the quote is wrong but because there is no commit to name — the file could change under the note with no trace. They are retained, and each affected section now says so, but they should not be treated as `[N]`: they are Hardman working papers, closer in kind to the first-hand material in `raw/16-presentation-registries.md` than to specification text. `refresh/sources.yaml` also lists `.ref` under `watch:` for this source, where it can never register a change; that is a config defect, not a quiet month.

## 0.1 What the 2026-09-15 pass covered

Nine commits touched `spec/` or `schemas/` between `84e7b68` and `b9227753c`. Three change what the specification says:

- **`4dae85a` (2026-06-09) replaced the `M`/`RM`/`Q`/`FIN` operator family with `MxN`/`RMxN`/`MxQ`/`RMxQ`.** This note never described the old family (see Warning 1), so nothing here is superseded by it. What it supersedes is what a reader of `main` would have said before June: that joint issuance uses an `M` operator whose threshold is an integer count in an `m` field, an `mgrp` attribute array enumerating potential signers, a separate `Q` qualification operator with `qschema` and `qev` fields, and a `FIN` **operator** in the edges block. All five of those are gone. Finalization is now an `fi` **field in the attributes section**, not an operator; qualification is folded into the two `…xQ` operators plus a `qs` field; and the operator name itself now carries the issue-versus-revoke distinction.
- **`e99878d` then `ff5b0a4` (both 2026-06-09) changed the threshold field twice in one day.** `e99878d` unified an integer threshold under a single field name `m` across the four operators ("the group is satisfied when enough slots carry valid endorsements to meet a numeric threshold", `dossier-spec-body.md` §"Threshold mechanics", L353 at `e99878d`). `ff5b0a4` then removed `m` entirely in favour of weighted-unity thresholds. **Only the endpoint is citable**; the intermediate `m` field never reached `b9227753c` and no claim should rest on it.
- **`b97c2f2` and `b922775` (both 2026-08-25) distinguish anchoring from signing throughout the spec and the schemas.** This is the largest doctrinal change in the window and it reaches almost every section of this note. It added a new normative section, §"How a Dossier Is Issued: Anchoring, Not Attached Signatures" (recorded as §1A below), and recomputed both schema SAIDs as a consequence — a wire-format change, not an editorial one.

Two further commits are structural rather than doctrinal. **`c2d261c` (2026-06-30)** deleted `spec/terms-definitions/qualification-operator.md` and linked live glossary terms; this note never cited that term, so nothing is superseded (negative result). **`9d8f9a0` (2026-06-09)** consolidated `schemas/endorsement.json`, `schemas/declination.json` and `schemas/qualified-endorsement.json` into one `endorsement.json`; this note already described a single endorsement schema distinguished by `disp`/`act`/`e.qp`, so again nothing is superseded — but the schema's SAID changed, see §5.

**`7aeadc2` (2026-08-06, henkvancann) — negative result, stated deliberately.** The commit subject promises "amendments and comments to the Dossier spec," and the body file did change. Read at the source rather than from the subject line, the body change is six deleted lines, all of them Spec-Up-T rendering directives of the form `[//]: # (\newpage)`, with no prose content. The substantive work went into `spec/dossier-spec-head.md`: inline reviewer queries in `{@DH …}` braces, several rewordings, and one new heading ("What's the challenge in aggregating Verifiable Evidence?"). **`33ef69d` (2026-08-12, "Editorial cleanup following #23") reverted essentially all of it** — the inline comments, the reworded headings, and an abstract that had gained "and contextual display" are all absent at `b9227753c`. What survives from this contributor at the pin is the Contributors credit: "[Henk van Cann](https://github.com/henkvancann), [KERI Foundation](https://keri.foundation/)" (`dossier-spec-head.md` §"Contributors", L16). No normative claim in this note is affected.

**`5906e8c` (2026-08-12) normalized heading capitalization to title case, so every `§heading` citation in this note has been re-anchored against the full heading list at `b9227753c`.** Headings are now cited in full, exactly as they appear. Six of the note's old short-form anchors were also *incomplete* rather than merely re-cased — `§"Core Structure"` is really "Core Structure: The Dossier as an ACDC", `§"The Edges Attribute"` is "The Edges Attribute: Linking to Evidence", `§"The Attributes Section"` is "The Attributes Section: Proximate Metadata", `§"The Operational Lifecycle"` is "The Operational Lifecycle: Creation, Evolution, and Verification", `§"Verifier Trust"` is "Verifier Trust and Root of Trust Management", `§"Use Cases"` is "Use Cases and Architectural Patterns". Two headings genuinely re-cased under sections this note cites: "Long-term Auditability and Historical Analysis" → "**Long-Term** Auditability…", and "Integrity and Non-Repudiation **Via** KERI" → "…**via** KERI". One heading was *renamed* for doctrinal reasons rather than cased: "Curation: Assembling and **Signing** the Dossier" → "Curation: Assembling and **Issuing** the Dossier" (`b97c2f2`). And the normalization is not complete: `#### Slot dispositions` (L430) is still sentence case at `b9227753c`, alone among the body's headings — a low-stakes residual, but a reason not to assume title case when re-anchoring next month.

Spec status at the pin: "**Specification Status**: v0.6 Draft" (`dossier-spec-head.md`, L3).

---

## 1. What a Dossier fundamentally IS / IS NOT (worldview & design intent)

- **A dossier IS itself an ACDC.** "A dossier MUST be a valid Authentic Chained Data Container (ACDC)" (`spec/dossier-spec-body.md` §"Core Structure: The Dossier as an ACDC", L5 at `b9227753c`). It is not a new container format — it is a *usage pattern* of ACDC.
- **A dossier is a curated BUNDLE of evidence, not a set of direct claims.** "The primary payload of a dossier is not a set of direct claims, but rather a graph of references to external evidence" (§"The Edges Attribute: Linking to Evidence", L87). The substance of the assertion enters *exclusively through edges*.
- **A dossier has an issuer and no issuee, and is an affidavit rather than a credential.** "It functions as a cryptographically verifiable affidavit about the composition and integrity of that collection rather than as a credential about a subject" (`spec/terms-definitions/dossier.md`, L3). The distinction is developed at length in the front matter: "A dossier, by contrast, has no issuee. It has only an issuer—the entity that curates the collection" (`spec/dossier-spec-head.md` §"Introducing the Dossier: An Issuer-Centric Evidence Container", L56).
- **The dossier's `a` (attributes) section is inverted vs. a normal credential.** In a conventional credential the `a` section carries the issuer's claims about a subject; in a dossier the substance is the evidence graph and "The `a` section MUST NOT be used to carry primary evidence" — it is reserved for *proximate metadata* (facts about the dossier itself) (§"The Attributes Section: Proximate Metadata", L313). Concrete illustration: crash photos/diagram/witness statements are edges (evidence); the adjuster's name, case number, assembly date, governance framework are `a`-section metadata.
- ~~**What the issuer's signature attests is composition/integrity, NOT veracity.**~~ **Superseded 2026-09-15 by `b97c2f2`; the veracity half stands, the signature half does not.** There is no issuer signature on a dossier. What attests composition is the issuer's *anchor*: "The issuer of a dossier is the entity that curates the collection of evidence and attests to its composition by anchoring the container" (§"The Role of the Issuer", L83). The veracity boundary is unchanged: "The issuer does not necessarily attest to the veracity of the claims within the evidence, but rather to the integrity and composition of the collection itself" (§"The Role of the Issuer", L83). What the anchor asserts is still: at issuance time, this collection is exactly the collection the issuer intended to present. See §1A.
- **A dossier is intentionally HEAVY, designed for REUSE.** "A dossier is intentionally heavy" and "is designed for reuse: curation happens once, and the resulting artifact serves as an authoritative reference for many later transactions, verifiers, and decisions" (§"Dossiers and Derivative References", L465). Curation is amortized across many transactions.
- **Dossiers are persistent, evolving artifacts** with a lifecycle: "curation, iterative assembly, state management, citation, and verification" (§"The Operational Lifecycle: Creation, Evolution, and Verification", L242).
- **Design purpose:** aggregate "an arbitrary quantity and variety of evidence" (§"The Edges Attribute: Linking to Evidence", L89); a dossier MAY contain an unbounded number of edges.

## 1A. Anchoring, not attached signatures — the spec's own correction [N, new at `b9227753c`]

This section did not exist at the previous pin. `b97c2f2` (2026-08-25) added a normative block that states the issuance mechanism once and then enforces the vocabulary through the rest of the document; `b922775` carried the same correction into the two schemas and recomputed their SAIDs. **This is corroborative evidence, from a standardizing specification, for `keri-doctrine.md` claim 8 and for the shibboleth row "A credential is a signed envelope (paired signature)" — see §15.**

- **The flat denial.** "A dossier is not a signed document. The issuer does not compute a signature over the dossier ACDC and attach that signature to it as proof of authenticity" (§"How a Dossier Is Issued: Anchoring, Not Attached Signatures", L13-14). Restated for the VC audience in the front matter: "A dossier is not signed; nothing is attached to it. The issuer's commitment lives in an append-only log" (`spec/dossier-spec-head.md` §"Relationship to W3C Verifiable Presentations", L95).
- **The four-step mechanism** (§ same, L18-25): the assembled dossier is saidified; the issuer constructs a seal containing that SAID; the issuer anchors the seal in an append-only verifiable log, "either directly in the key event log (KEL) of the issuer's AID, or in a transaction event log (TEL) for the dossier"; and "The key event carrying the anchor is signed with the keys authoritative for the issuer's AID at that point in the log."
- **Where the signature actually lives.** "The signature exists on the anchoring key event, not on the dossier. What binds the issuer to the dossier is the anchor: a commitment to the dossier's SAID" (L27-28), placed in a log "that is append-only, duplicity-evident, and witnessed" (L29).
- **Why, stated as the retrograde problem without using the word.** "An attached signature can be checked only against the key that produced it, and nothing in the signature itself establishes when it was made" (L32-34); "once that key is rotated or compromised, a verifier has no way to distinguish a signature made while the key was authoritative from one made afterward" (L34-36). By contrast an anchor "is evaluated against the key state the KEL shows was authoritative at the position where the anchoring event sits, so it stays verifiable across any number of later rotations" (L36-39).
- **It is framed as a requirement, not a preference.** "Because a dossier is expected to be verified years or decades after issuance by parties who cannot reach the issuer, anchoring is the only mechanism that satisfies the requirement" (L39-41).
- **Three defined terms, now used precisely throughout** (L45-55). To **anchor** is "to commit its SAID into an append-only verifiable log by placing a seal containing that SAID in an event of that log"; an anchored artifact "carries a non-repudiable and perpetually verifiable commitment from the AID that controls the log". **Signed** "means bearing an attached digital signature computed over an artifact's own bytes" — and the spec then partitions its own artifacts: "key events are signed, and so are the short-lived transactional objects described under *Dossiers and Derivative References*. Dossiers, endorsements, and other ACDCs are anchored." **Authenticated** is "used where an obligation is satisfied by either mechanism".
- **The one narrow exception, and its price.** §"Ephemeral Dossiers With Attached Signatures" (L57-79): a dossier that "lives only for the duration of a single interaction, and that will never be verified after the issuer's next key rotation, MAY be authenticated by an attached signature" (L59-62). Three consequences are spelled out — the dossier "ceases to be verifiable once the signing key is no longer part of the current key state"; "The issuer's commitment becomes repudiable in practice. Absent a log entry, there is no duplicity-evident record that the issuer ever made it"; and verification at a referenceTime other than the present "is not possible". An attached signature is therefore "NOT RECOMMENDED for any dossier that is published, cited, cached, or otherwise expected to outlive the transaction that produced it" (L75-77), and "A verifier MUST NOT treat an attached signature as equivalent to an anchor when evaluating a dossier as of any referenceTime other than the present" (L77-79).
- **A consequence for redaction that the old framing obscured.** "Because what the issuer anchored is that top-level SAID, redaction leaves the issuer's commitment intact" — and "had the issuer signed the container's bytes, any redaction would have invalidated that signature" (§"Graduated Disclosure Mechanism", L533). Graduated disclosure is thus presented as *downstream of* the anchoring choice, not as an independent ACDC feature. See §8.
- **Terminology hygiene is the stated motivation.** "Terminology in this area is easy to get wrong, and the wrong reading has real consequences for implementers" (L9-10). Worth noting for the corpus's own sake: this is a specification author deciding that the signing/anchoring conflation is the failure mode most likely to bite an implementer arriving from the VC world.

## 2. Invariants and "never do X" rules (dossier spec)

Normative MUSTs / MUST NOTs / SHOULDs (all at `spec/dossier-spec-body.md`, `b9227753c`, unless noted):
- Dossier **MUST** be a valid ACDC.
- Schema **MUST** be an `allOf` array with one member a `$ref` to the base dossier schema *by SAID*. The `$ref` "signals that the schema is for a dossier and should be processed using the dossier semantics defined in this spec" (§"Base JSON-Schema Definition", L98). **Base dossier schema SAID superseded 2026-09-15 by `b922775`:** ~~`ECpZgR2Ybj2QCfFZrnNQUqhbLnZuMl5cjL2MtgDaxMbY`~~ → **`EKuLzS_oN_mIao5og1NOujtF2QMXloiMCZP7xuAR5tY7`** (`schemas/dossier-base.json`, `$id`, L3). The recomputation was forced by rewriting the schema's prose descriptions in anchoring rather than signing terms — which means it is a **wire-format change, not an editorial one**: any schema whose `allOf` `$ref` names the old SAID no longer resolves, and any dossier validating against it is affected. The schema's own IS-A rule is worth recording verbatim: "A schema S IS-A dossier iff S's top-level allOf array contains a $ref to this schema's SAID, or recursively to another schema that IS-A dossier" (`schemas/dossier-base.json`, `description`, L5).
- **MUST** use the ACDC `e` (edges) section to bind the dossier to all *evidenta*, and **MUST NOT** place any evidenta in the `a` section.
- **MUST** define `a`-section fields for proximate-metadata.
- **SHOULD NOT** include an issuee field (a dossier is issuee-less).
- **SHOULD** set `additionalProperties: true` at root and for the edges object, so issuers can add arbitrary application-specific edges without invalidating against the base schema.
- **MAY** include ordinary (non-evidenta) ACDC-relationship edges too.
- Foreign Artifact wrapper **MUST** be a valid ACDC with no issuee; its `a` section **MUST** contain `content_digest` (CESR-encoded hash) and `content_type` (IANA MIME type); `content_digest` **SHOULD** be a bSAID or xSAID.
- Derivative **MUST** cryptographically bind to the dossier; a derivative **MUST NOT** be treated as independent evidence — "its authority derives entirely from the dossier, and its trust value collapses to that of the dossier alone if the binding cannot be checked" (§"Dossiers and Derivative References", L488). **The binding mechanism was superseded 2026-09-15 by `b97c2f2`:** ~~"minimally by including the dossier SAID under the derivative's signature"~~ → "minimally by including the dossier SAID under whichever mechanism authenticates the derivative: the signature over a token's payload, or the resolvable identifier that constitutes a citation" (L488). The generalization matters because only one of the two derivative forms bears a signature at all.
- When evaluating a derivative, a verifier **MUST** consult the dossier's revocation state effective at *verification time*, not derivative-issuance time. "A token issued before its referenced dossier was revoked is not valid after the revocation event, even if the token's own `exp` has not yet passed."
- Any protocol citing a dossier **MUST** incorporate ephemeral, context-specific signed data (timestamps, originator/destination IDs, nonces) to defeat replay.
- To manage evidence state transitions, dossiers **MUST** use Annotation Edges (because ACDCs are immutable, an issuer "cannot simply modify the metadata of an existing edge"). Verifiers **MUST** traverse the graph to resolve the "effective state" of each evidence item, applying the latest annotations.
- To include dynamic data, issuers **MUST** use Temporal Pinning (never link a live API directly — "Direct links to live APIs are unverifiable in a static context").
- An active "no" in joint issuance **MUST** be an *anchored* declination. **Superseded 2026-09-15 by `b97c2f2`:** ~~"never as a null or unsigned slot"~~ → "An active 'no' MUST therefore be expressed as an anchored declination, never as a null or unanchored slot" (§"Slot dispositions", L437). The doctrine is unchanged and the mechanism is renamed: a pending slot and an absent slot remain equivalent in trust terms, because "only the candidate's anchor authenticates the candidate's decision" (L437) — where the previous wording said the candidate's signature.

**Anti-pattern explicitly corrected — direct reference to non-ACDC material:** "Direct reference to non-ACDC material without a wrapper is NOT RECOMMENDED, as it places an untenable burden on the verifier to parse and validate an arbitrary foreign format, understand its lifecycle, and locate its revocation mechanism" (§"Referencing Non-ACDC Evidence", L160-163 — re-anchored 2026-09-15; the old citation `§"Incorporating Evidence"` named the parent `##` section, and the text sits in the `###` subsection). RECOMMENDED approach: wrap foreign material in a new ACDC before linking.

## 3. Incorporating evidence — three evidence categories

1. **ACDC-native evidence** — edge value MUST be a JSON object with at least `n` (SAID of referenced ACDC — "direct, tamper-evident link") and `s` (SAID of its schema — lets verifier parse/interpret). Exemplified by VVP.
2. **Opaque file artifacts** (photos, audio/video, PDFs, genomic data, spreadsheets, binaries) — "cannot participate in authenticated data graphs using the standard ACDC saidification algorithm, because that algorithm assumes JSON content that can be canonicalized and rewritten." Solution: give the artifact a cryptographic identity via bytewise/externalized SAID algorithms, then issue a **Foreign Artifact ACDC** wrapper.
   - **bSAID (bytewise SAID):** for artifacts whose bytes *can* be rewritten (JPEG Exif, Markdown comment). SAID intrinsic to byte stream via a `SAID:` insertion point; recoverable by scanning raw bytes.
   - **xSAID (externalized SAID):** for artifacts that *cannot* be safely rewritten (compressed archive, encrypted file, PDF with cross-reference table). SAID carried in the filename under a constraint expressed via `XSAID:` delimiter inside the file.
   - Fallback: plain CESR-encoded hash in `content_digest` — "the integrity guarantee is weaker: the hash cannot be discovered by inspecting the artifact itself, only by consulting the wrapper."
   - CESR encoding of `content_digest` is self-describing — "the primitive code identifies the hash algorithm, so no separate algorithm field is required."
3. **Foreign credentials** (W3C VC, ISO mDL) — a designated **bridging party** verifies the foreign credential per its native rules, then issues a **bridge wrapper** ACDC attesting: "I, the bridging party, successfully verified the attached foreign credential on date X according to policy Y." Two caveats: (1) trust depends on the bridging party's reputation/security/policies; (2) "the revocation lifecycles of the original foreign credential and of the bridge wrapper are decoupled unless a specific governance framework explicitly links them." This "transforms the problem of verifying a foreign format into the problem of trusting the attestation of the bridging party."

## 4. Operational lifecycle (curation → citation → verification)

**Curation steps (normative)** — §"Curation: Assembling and Issuing the Dossier", L248-260. **The section heading itself was renamed from "Curation: Assembling and Signing the Dossier" by `b97c2f2`, and steps 4-6 were rewritten with it (superseded 2026-09-15).** (1) Evidence acquisition from authoritative sources; (2) Assembly — build edges block of named links; (3) Iterative assembly & versioning — new versions link to previous via a `prev` edge; "This creates a verifiable chain of the dossier's history, allowing verifiers to traverse back through the lineage of the evidence collection" (L254); (4) **Issuance initiation** — ~~single-issuer: collector signs~~ → "For single-issuer dossiers, the collector issues the fully assembled dossier ACDC **by anchoring it**" (L256); joint: collector hands the drafted ACDC to a coordinator, who "coordinates the endorsement and anchoring process among the designated members"; (5) **Anchoring** (~~"Signing & anchoring"~~) — the collector or finalizer "saidifies the dossier, places a seal containing that SAID in an event, and anchors the event in the key event log (KEL) of a KERI AID they control", and pointedly, "The private keys authoritative for that AID sign the anchoring key event, not the dossier itself" (L258). "This act creates a permanent, non-repudiable attestation to the dossier's content"; in joint issuance "the anchor may be distributed across several KELs or consolidated in a finalization event"; (6) Publication — ~~signed~~ → "the issued dossier ACDC" published at a stable, publicly resolvable location, typically HTTP URLs (L260).

**Roles (leader-follower joint issuance):**
- **Collector:** assembles evidence artifacts, defines initial dossier structure.
- **Coordinator:** once collection finished, initiates issuance action, distributes candidate dossier for endorsement.
- **Finalizer:** any entity that, observing threshold met, submits a finalization event to a KEL.
- Also **assembler** (curator; may differ from issuer), **oracle/observer** (temporal pinning), **bridging party**.

**State management — Annotation Edges** (§"State Management and Metadata Overlays", L264-266): an edge in a new dossier version pointing to an artifact (or edge) in a previous version, carrying the new state/ruling. Example: "Court Case Dossier v2" edge `ruling_101` points to SAID of `exhibit_A` (v1) with `status: "admitted"`.

**Temporal Pinning** (§"Temporal Pinning", L270-277): oracle observes dynamic state at Time T → wraps the observation in an Observation Attestation ACDC → anchors that ACDC in a KEL. Dossier links to the static timestamped attestation. "freezes" the data stream: asserts "had $50,000 at the exact moment this dossier was assembled" rather than "has $50,000 now." **Minor supersession 2026-09-15 by `b97c2f2`:** this note described the attestation as a ~~signed~~ ACDC; the glossary now reads "An ACDC, **anchored by its issuer**, produced during temporal pinning that records a dynamic value observed at a specific time" (`spec/terms-definitions/observation-attestation.md`, L3).

**Citation** (§"Citation: Referencing the Dossier in Protocols", L281-283): dossiers are "generally not transmitted in their entirety within real-time communication protocols. Instead, they are cited." A citation MUST be a resolvable identifier enabling fetch of the complete, unmodified dossier ACDC. Canonical implementation: **OOBI URL** in the `evd` (evidence) claim of a VVP passport. "An OOBI is a specialized URL that points to a resource serving the ACDC and its associated KERI proofs."

**Verification algorithm (inputs: citation + referenceTime)** — §"Verification: Algorithm for Validation", L287-305. **Step 4 was retitled and materially changed by `b97c2f2` (superseded 2026-09-15), and a new sub-step (d) was added.**
1. Fetch dossier (resolve citation).
2. Validate integrity — recompute SAID, match expected SAID from citation.
3. Determine issuance model — inspect `a` for `fi` (finalization identifier) and edges for joint-issuance threshold operator (`MxN`, `RMxN`, `MxQ`, `RMxQ`) in an edge group's `o` (L293).
4. **Validate anchors** (~~"Validate signatures/anchors"~~, L295): (a) if `fi` present/non-null, find the finalization event in that AID's KEL and verify it "carries the threshold-satisfying endorsements for the relevant operator" (L296); (b) if `fi` absent but a threshold operator is present, evaluate each slot — a slot is **Endorsed** only when it "references an Endorsement ACDC with `disp` `"endorse"` and `act` appropriate to the operation, issued by the expected endorser and **anchored** in that endorser's KEL" (L297; the word ~~signed~~ is gone). Endorsed weights must sum to at least unity, "for the qualified operators, using the uniform member weight the operator declares"; qualified operators additionally require each endorsement carry a qualification proof (`e.qp`) validating against the operator's `qs` schema; (c) **single-issuer — superseded, and the correction reverses the old rule.** This note previously said: verify the signature against keys authoritative *at the referenceTime*. The spec now says the opposite, and says why. Locate the event anchoring a seal containing the dossier's SAID, then verify that event's signatures "against the key state the KEL establishes as authoritative *at that event's position in the log*, not against the key state current at the referenceTime" (L298) — because "an anchor remains verifiable across any number of later rotations, and requiring the referenceTime key state would defeat that property" (L298). Then confirm the anchoring event precedes the referenceTime; (d) **new sub-step** — only for a dossier authenticated by an attached signature under the ephemeral exception (§1A), verify that signature against the issuer's current key state; "A verifier MUST reject such a dossier when the referenceTime is not the present", and SHOULD reject it when it "was retrieved from a cache or a published location rather than received directly within the transaction it authenticates" (L299).
5. Recursive graph traversal — for each edge, fetch referenced artifact and validate recursively.
6. Check revocation status — for the dossier and *every node* in the graph, consult KELs/status registries for revocation effective at the referenceTime.
7. Apply semantic rules — application-specific policy after cryptographic validation completes.

**Layered verification doctrine** (§"Base JSON-Schema Definition", L127)**:** verification is a two-layer process. Layer 1 = cryptographic validation, universal, defined by this spec — **superseded 2026-09-15 by `b97c2f2`:** ~~"confirming signatures, SAIDs, and KEL consistency"~~ → "confirming **anchors**, SAIDs, and KEL consistency". Layer 2 = semantic validation, "necessarily application-specific and requires context-dependent business logic." A generic verifier "can verify that an edge labeled 'lunarPropertyDeed' is cryptographically linked, but it cannot know what that means or how to process it." This separation is what makes the dossier a "universal building block for evidence aggregation."

## 5. Joint issuance (the doctrinal heart of multi-party dossiers)

**Tier note, 2026-09-15.** Everything in this section was mined at `3900e62`, a commit on the side branch `schema-base-dossier` that was never an ancestor of `main` (see §0). At `b9227753c` the same content is on `main` and is genuinely `[N]`. A reader of the bible who built against `main` before 2026-06-09 would have implemented the older `M` / `RM` / `Q` / `FIN` family instead: an integer count in an `m` field, potential signers enumerated in an `mgrp` attribute array, a separate `Q` operator carrying `qschema` and `qev`, and a `FIN` operator in the edges block. Nothing below describes that model; everything below replaces it.

- **Framing** (§"Logic", L408-414): joint issuance is "not a single, uniform approach to approval, but a family or style of approval strategies" mapping onto "coordinated control in multi-agent systems" studied in robotics/AI/military science. Three cooperative-control variants cited: **leader-follower**, **behavior-based control**, **virtual structures**.
- **Key contrast vs. group multisig** (§"Logic", L414): "Unlike group multisig, which requires synchronous agreement on key event log (KEL) sequence numbers, joint issuance relies on logic within the ACDC layer." Validity is decoupled from key management → more flexible issuance/verification. **The next sentence was superseded 2026-09-15 by `b97c2f2`:** ~~"This lets members contribute signatures or seals to a dossier at different times and via different channels without immediate impact on a shared KEL."~~ → "This lets members **anchor their endorsements** of a dossier at different times and via different channels, **each in their own KEL**, without immediate impact on a shared one." The correction sharpens the claim rather than weakening it: members are not adding signatures to a shared object at all, they are each writing to a log they alone control.
- **Threshold mechanics** (§"Threshold Mechanics", L426-428): a weighted threshold operator in the `o` field of an edge group; member edges are **slots**; each slot carries a weight in reserved `w`; group satisfied when weights of Endorsed slots sum to **≥ unity (1)**. "This is the same fractionally weighted threshold KERI uses for key-event signing thresholds (`kt`): the threshold itself is the fixed constant 1, so there is no separate count field." For m-of-n equal endorsers, each of n slots gets weight `1/m`, "so that any *m* of them sum to unity while any *m*−1 fall short" (L428). Unequal weights express weighted governance (senior endorser `2/m`); grouped weights express nested AND/OR-of-threshold rules, "exactly as KERI's nested `kt` lists do" (L428).
- **What "weighted unity" means, and the field it replaced.** This is the endpoint of two same-day commits (`e99878d`, then `ff5b0a4`, both 2026-06-09) and **only the endpoint is citable**. `4dae85a` had introduced the four operators with a numeric threshold; `e99878d` unified that threshold under one field name `m`; `ff5b0a4` then deleted the field. The design now has **no threshold field at all**: the threshold is the fixed constant 1, and "how many endorsements are enough, and how much each is worth, lives entirely in the weights" (§"Threshold Mechanics", L426). The relation to KERI is identity, not analogy — the spec names `kt` and says it is "the same fractionally weighted threshold". So a reader who already understands KERI's fractionally weighted signing thresholds (`raw/01-keri-spec.md`, `bible/03-key-management-and-identifier-lifecycle.md`) needs no new mechanism for dossier joint issuance, only a new placement: weights sit on edge slots in the ACDC layer instead of on keys in the KEL. The glossary states the same: "the operator is satisfied when the weights of its endorsed slots sum to at least unity (1) — the same fractionally weighted threshold KERI uses for `kt`" (`spec/terms-definitions/threshold-operator.md`, L3).
- **A schema may fix the rules or defer them** (§"Threshold Mechanics", L428): "A schema MAY fix the operator, the weights, and the set of candidate endorsers, or MAY defer some or all of these to the dossier instance."

**Slot dispositions — only an *anchor* authenticates a decision** (§"Slot dispositions", L431-437; this heading is still sentence case at `b9227753c`, the sole survivor of `5906e8c`'s title-case pass). **The whole subsection's mechanism vocabulary was superseded 2026-09-15 by `b97c2f2`: "signed" → "anchored" in every disposition.** A slot points via `n` to an endorsement ACDC, names the schema that endorsement must satisfy via `s`, assigns a weight via `w`, and identifies the expected endorser "by the issuer (`i`) of the ACDC the slot references" (L431).
- **Pending:** ~~references an unsigned meta ACDC naming the candidate but carrying no signature~~ → "references a placeholder meta ACDC that names the candidate endorser but that the candidate **has not anchored** (or the slot is null)" (L433). Contributes nothing; records only that an endorsement is anticipated.
- **Endorsed:** ~~references a signed Endorsement ACDC~~ → "references an endorsement ACDC issued by the candidate and **anchored in the candidate's KEL**, with a `disp` (disposition) of `"endorse"` and a `said` attribute equal to the dossier's SAID" (L434). Weight added to sum.
- **Declined:** same endorsement ACDC, ~~signed~~ → anchored, but `disp` `"decline"` — a declination. Weight not added, but "records attributable dissent — distinguishing a candidate who was asked and refused from one who has not yet acted" (L435).
- Doctrine (L437): "a pending slot and an absent slot are equivalent in trust terms: neither attributes any act to the candidate." ~~"An active 'no' MUST therefore be expressed as a signed declination."~~ → "An active 'no' MUST therefore be expressed as an anchored declination, never as a null or unanchored slot."
- **The schema says the same thing more bluntly than the prose does**, which is worth having: an endorsement "is authenticated by anchoring... It is not signed and carries no attached signature" (`schemas/endorsement.json`, `description`, L5). And an unanchored instance "is only a placeholder reserving a slot for a named candidate; its field values are not authoritative until anchored" (same).

**Four threshold operators** (§"Threshold Operators", L440-449), distinguished by `disp` (endorse/decline), `act` (issue/revoke), and the optional qualification-proof edge `e.qp`. All four use one endorsement schema — **SAID superseded 2026-09-15 by `b922775`:** ~~`EAfn0gRMUnp6d1hyE5qJCN86kBFBp80JwMdm0BqiC1B0`~~ → **`ECRjgun8t3cay_YBiVxEY-qXKCz37oUrvE5GvmySwxCh`** (`spec/dossier-spec-body.md` §"Threshold Operators", L442; confirmed against `schemas/endorsement.json` `$id`, L3). As with the base schema, this is a wire-format change: a slot's `s` field naming the old SAID no longer resolves. The consolidation into a single schema dates from `9d8f9a0` (2026-06-09), which merged `endorsement.json`, `declination.json` and `qualified-endorsement.json` — the three-schema arrangement never appeared in this note and nothing here is superseded by that commit. The operator's own name carries the issue/revoke distinction: "The operator's own name, together with the `act` of the endorsements it counts, distinguishes issuance from revocation" (L442). The `act` field exists to stop cross-replay: "Binding the act prevents a disposition toward issuance from being replayed as one toward revocation, or vice versa" (`schemas/endorsement.json`, `a.act` description, L66).
- **`MxN`** ("M of N") — issuance operator, exactly N slots (one per candidate); each counted endorsement carries `act` `"issue"`, `disp` `"endorse"`, omits `e.qp`. Candidates enumerated structurally as slots (each names its expected endorser via the issuer `i` of the referenced ACDC) — "embodies an *m of n* pattern without any separate enumeration of potential endorsers" (L444; ~~"potential signers"~~ was superseded 2026-09-15 by `b97c2f2`). Example: judicial decision by m of n named justices.
- **`RMxN`** ("Revocation M of N") — same mechanics, `act` `"revoke"` (L445). Revocation slot set MAY be identical/overlapping/disjoint from issuance slots; weights MAY differ — "the authority to revoke can be configured independently of the authority to issue."
- **`MxQ`** ("M of Qualified") — issuance for an *open-ended* set of qualified endorsers; slot count NOT fixed in advance (slots added as qualified endorsers act) (L446). Declares a *uniform member weight* in its own `w`; "the group is satisfied when those weights sum to unity (equivalently, when at least `1/w` qualified endorsers have endorsed)". Each counted endorsement carries `act` `"issue"`, `disp` `"endorse"`, and a qualification-proof edge `e.qp`. Edge group MUST carry a `qs` field naming the SAID of the schema each qualification proof must satisfy, chosen by the dossier creator "since the proof of qualification differs from one context to another". Models "an endorsement open to anyone who can prove they are qualified — for example, 'any licensed physician in good standing.'"
- **`RMxQ`** ("Revocation M of Qualified") — same, `act` `"revoke"` + `e.qp`; qualified revoker set/weights configurable independently (L447).
- **Declination under any of the four** (L449): "A **Declined** disposition under any of these operators is the same endorsement ACDC, anchored with `disp` `"decline"` and the matching `act`." Whether a declination must also carry a qualification proof under the qualified operators "is a policy choice left to the governing schema"; a declination never adds weight in any case. Recorded because it is an open configuration point the spec deliberately does not close.

**Finalization (`fi` field)** — §"Finalization", L452-455: optional aid to verifiers that don't do recursive traversal, "signaled by the `fi` ('finalization identifier') field in the dossier's attributes (`a`) section, rather than by an edge operator" (L452). This is the residue of the retired `FIN` **operator** (`84e7b68`, superseded on `main` by `4dae85a`): finalization moved out of the edges block into the attributes block and stopped being an operator at all. When `fi` present/non-null → holds the AID (typically a group AID) whose KEL is expected to carry the finalization event, where a finalizer "anchors the threshold-satisfying proofs... so that the aggregate evidence is collected in one predictable place"; verifier SHOULD use it as definitive proof. When absent/null → "no finalization event is promised. A verifier MUST instead gather the endorsements from the participants' individual KELs and confirm directly that the threshold is met."

**Revocation** — §"Revocation", L458-461: defined independently of issuance, "using the `RMxN` or `RMxQ` operator". Default: "if no revocation operator is present, the threshold required to revoke a dossier is identical to the threshold required to issue it." Asymmetric thresholds allowed — "a dossier may require a majority for issuance (equal weights) but let a single administrative AID revoke (that slot weighted at unity on its own)".

## 6. Derivatives — Citation vs. Token (separating two conflated questions)

Two derivative forms:
- **Citation:** resolvable identifier (canonically OOBI URL) → fetch full dossier + run algorithm.
- **Token:** short-lived signed object carrying enough context for an immediate decision, embedding the dossier SAID as evidence pointer (`evd`). Sample fields: `iss`, `iat`, `exp`, `aud`, `nonce`, `evd`. "The token asserts that, between `iat` and `exp`, its bearer is acting under the authority of the dossier whose SAID is given in `evd`" (§"Dossiers and Derivative References", L486). A verifier with a cached validated dossier MAY accept without re-traversing; one requiring fresh assurance dereferences `evd`, runs full algorithm, caches result.
  - **New at `b9227753c` (`b97c2f2`), and the reason the token is the exception:** "A token is one of the few artifacts in this specification that genuinely bears an attached signature... it can afford one because its lifetime is measured in seconds and it is never expected to outlive a key rotation" (L473). So the spec's signed/anchored partition (§1A) is drawn on lifespan, not on artifact kind: the things that are signed are key events and sub-minute transactional objects, and everything meant to survive a rotation is anchored.

The split separates two questions "conflated in conventional bearer credentials":
- *What was attested, and by whom?* — lives in the dossier, curated once, amortized.
- *Who is presenting it now, in what session, under what immediate constraints?* — lives in the derivative, bound to the transaction via ephemeral fields (`iat`, `exp`, `aud`, `nonce`).

Derivatives inherit the dossier's revocation lifecycle (evaluated at verification time).

## 7. Security & threat-model positions

**Provenance warning (2026-09-15):** the bullets marked "(primer …)" below come from `.ref/keri-primer.md`, which is **untracked and gitignored** (§0, Warning 2). They cannot be pinned to any commit and should not be treated as `[N]`. The bullets marked "(dossier …)" are from the spec body and are pinned to `b9227753c`.

- **Detection over prevention / duplicity-evident (primer §2.6 — unpinnable):** "Duplicity is KERI's term for the double-spend problem in identity" — a controller signing two different events with the same sequence number. Witnesses provide availability + duplicity detection; **watchers** poll witnesses and, if two witnesses report different head hashes for the same sequence, "the watcher has detected duplicity... can broadcast the conflicting events as cryptographic proof of fraud... this proof is fatal to the reputation of the identifier." Doctrine quote: "Detection (rather than prevention) allows KERI to operate with low latency while ensuring that any dishonesty is provable."
- **Adversarial / malicious-controller trust model:** "The trust model is adversarial: witnesses are assumed to be potentially malicious" (primer §2.6). Witnesses ≠ CAs: "A witness makes no such assertion. A witness simply stores and serves events" — a CA is trusted to attest to identity; a witness is not.
- **Decentralized / plural root of trust (dossier §"Verifier Trust and Root of Trust Management", L517-519 — re-anchored 2026-09-15; the old short anchor `§"Verifier Trust"` was incomplete, not re-cased):** "The dossier model operates on a decentralized root of trust. A verifier does not rely on a single authority but makes explicit trust decisions about a plurality of evidence issuers." In joint issuance trust is distributed across member AIDs. Verifiers SHOULD consult multiple witnesses "to ensure they have a consistent and complete view of an issuer's KEL, thereby protecting against duplicity and compromise."
- **Distributed root of trust in VVP (Compositional Dossier):** "Trust is derived from the leaf nodes (the authorities), not merely from the dossier issuer." The assembler does not generate the evidence — bundles credentials from distinct domain-specific roots of trust (LEI issuer for legal identity, carrier/regulator for number authority, trademark steward for brand).
- **Integrity via SAID (dossier §"Integrity and Non-Repudiation via KERI", L505 — heading re-cased by `5906e8c` from "…Non-Repudiation **Via** KERI"):** "A SAID is a cryptographic hash of an object's canonical content. Any modification to the data results in a different SAID, making tampering immediately evident."
- ~~**Non-repudiation via KEL anchoring:** signatures "cryptographically anchored in a key event log (KEL), which serves as a permanent, publicly auditable, and tamper-evident log."~~ **Superseded 2026-09-15 by `b97c2f2`** (§"Integrity and Non-Repudiation via KERI", L507). It is not that signatures are anchored; it is that nothing about the dossier is signed. "**Anchoring** makes the act of issuing a dossier non-repudiable. The issuer commits the dossier's SAID into a key event log (KEL)... by placing a seal in a key event that the issuer's authoritative keys sign." And explicitly: "The signature is on that key event rather than on the dossier; what a verifier relies on is the presence of the commitment in a log the issuer cannot rewrite and witnesses can attest to." In joint issuance, "the collective anchors of all participating members in their respective KELs provide non-repudiation."
- **Pre-rotation firewall (primer §2.3 — unpinnable):** "Pre-rotation establishes a firewall between day-to-day use and occasional governance." Current key K1 on a production server; next key K2 generated, hashed, stored air-gapped. "If the server is breached, the attacker steals K1 but cannot rotate to assume control of the identity." Security "comes from the asymmetry in knowledge: the controller knows or has access to K2, while the world only sees its hash." In standard PKI "there is no defense against this; a signature from the compromised key looks exactly like a signature from the legitimate owner."
- **Historical / point-in-time verifiability (dossier §"Long-Term Auditability and Historical Analysis", L523-525 — re-anchored 2026-09-15; `5906e8c` re-cased "Long-term" → "Long-Term"):** because KELs give a complete sequenced history, "a verifier can perform validation for any arbitrary point in the past" (L523). And: "An auditor can determine if a dossier and its entire evidence graph were valid at the time of a transaction, based on the key states and revocation information known at that moment" (L525). (Observer-relative / referenceTime-dependent validity.) The glossary now states the same property as the reason the input exists: "Because KERI preserves historical key state, a reference time lets a verifier determine whether a dossier was valid as of an arbitrary past moment, not only the present" (`spec/terms-definitions/reference-time.md`, L3).
- **Anchored (not paired) signatures & retrograde attacks (primer §3.3 — unpinnable; but see §1A for the same argument made in the spec body at `b9227753c`, which *is* pinnable):** ACDCs use anchored signatures — a provable KEL association independent of container context. "The credential was issued *during* the active window of a specific key... This eliminates timestamp ambiguity." Cites NIST SP 800-102: a signed message "provides no assurance that the private key was used to sign the message at that time." Without native anchoring, "simple paired signatures are vulnerable to 'retrograde attacks,' where a compromised key is used to forge historical events."
- **Replay mitigation (dossier §"Replay Attack Mitigation in Citation Protocols", L511):** the dossier is stable/long-lived, so "the primary risk of replay attacks exists at the level of the protocol that cites it."

## 8. Privacy doctrine

- **Graduated disclosure (dossier §"Graduated Disclosure Mechanism", L531-533 — re-anchored 2026-09-15; the old anchor `§"Graduated Disclosure"` was incomplete):** ACDC SAID is computed recursively — "the hash of a parent object is derived from its scalar values and the SAIDs of its child objects" (L531). **Superseded 2026-09-15 by `b97c2f2`:** ~~"any child object within an ACDC can be replaced by its SAID without altering the SAID of the parent object **and without invalidating the digital signature**"~~. The trailing clause is gone, because there is no signature to invalidate. The replacement makes redaction a *consequence* of anchoring: "Because what the issuer anchored is that top-level SAID, redaction leaves the issuer's commitment intact: the redacted form still hashes to the SAID sealed in the issuer's KEL. This is a further consequence of anchoring rather than signing — had the issuer signed the container's bytes, any redaction would have invalidated that signature" (L533). The joint-issuance corollary also gained a word: "In joint issuance, redaction does not affect the validity of member seals **anchored in KELs**, as those seals point to the immutable SAID of the root dossier" (L533).
- **Selective disclosure (primer §3.5 — unpinnable):** payload structured into sections, each with a random salt + digest; holder reveals only chosen sections via Merkle-proof style strategy.
- **Right to be forgotten (primer §3.5 — unpinnable):** "the KEL stores only the anchors (hashes) of the credentials"; payload is off-chain. Deleting the payload leaves an irreversible hash; identity history intact, personal data removed. Contrasted with blockchain: "'forgetting' an identifier requires a hard fork of the entire chain."
- **Correlation vectors (dossier §"Analysis of Data Correlation Vectors", L537-541):** (1) Dossier SAID — persistent unique identifier; (2) Citation signer AID — links all messages signed by same identifier; (3) Explicit brand information — intentional correlator.
- **Mitigations (dossier §"Mitigation Strategies for Unwanted Correlation", L545-549, and §"Contractually Protected Disclosure", L553):** rotate the citation-signing AID frequently (independent of the long-lived issuer AID); maintain a pool of AIDs for "herd privacy"; use a trusted blinding service that verifies an original dossier and issues a short-lived derivative attesting validity "without revealing its SAID to the end verifier." Also contractually protected disclosure — serve redacted version to anonymous requests, require signed request (tied to T&Cs) for expanded version, creating an audit trail of who accessed sensitive data.

## 9. Anti-patterns / outsider-tells the material explicitly corrects (from primer)

**Provenance warning (2026-09-15): this entire section is sourced from `.ref/keri-primer.md`, which is untracked and gitignored (§0, Warning 2). None of it is pinnable to a commit and none of it is `[N]`.** It is a Hardman working paper, and the same arguments appear in the tracked papers corpus (`/home/daniel/code/me/papers/x509-prob.md`, `was.md`) — synthesis should prefer those, which have commits. The one exception is the VC/VP contrast, for which a pinnable `[N]` source now exists in the spec's own front matter; see §14A.

These are the doctrinal reframings against PKI/blockchain/VC priors:

- **PKI/X.509 root-of-trust flaw:** "The fundamental flaw... is the separation of the identifier from the cryptographic keys that control it." In X.509 an identifier "is a lease entry in a database"; binding is "merely an assertion — a digital certificate — signed by a third party," creating "a root of trust that is external to the identity itself." KERI reframing: "The identifier is the root of trust" — binding must be "mathematical (and therefore, objectively provable and permanent), not administrative."
- **CA fragility:** "If a CA is compromised, coerced, or negligent, it can issue valid certificates that assert bindings that are not true" (DigiNotar, Symantec distrust, TrustCor). "the security of the leaf (the user) is entirely dependent on the security of the root (the administrator)."
- **Jurisdictional non-universality of CA trust:** "a CA-based ecosystem has different trust profiles in different jurisdictions" (SHAKEN not accepted in Europe/Asia). The CA/Browser Forum is "a new centralization."
- **Revocation-list critique (CRL/OCSP):** "X.509 standard also lacks a native, low-latency, enforceable mechanism for revocation." CRLs "heavy, bandwidth-intensive... scale poorly." OCSP "requires the client to query a central responder for every validation, creating a privacy leak (the CA knows every site you visit)." Fail-open: "browsers often 'fail open', meaning they accept the unverifiable certificate as valid." KERI requirement: "Revocation is immediate and safe... without privacy leaks or 'fail open' risks."
- **CT is detection not prevention, and re-centralizes:** "CT is a detection mechanism, not a prevention mechanism; fraudulent certificates may be usable for a time." And "the implementation of CT has introduced a new form of centralization."
- **The persistence / identity-continuity problem:** certificates expire/rotate to a "mathematically unrelated" new cert; "The only link between them is the administrative procedure of the CA." ACME continuity "exists only in the eyes of the CA." Requirement: "Identity survives key rotation... without having to trust a party who claims it's safe to ignore a gap."
- **KERI is NOT a blockchain (rejection of global consensus):** "Traditional blockchains rely on a global consensus model, where every node must agree on the total ordering of all transactions." "KERI rejects the need for global consensus regarding identity. Instead, it uses microledgers called key event logs (KELs)." "The ordering of events matters only relative to that specific identifier." "KERI naturally scales horizontally because there is no central choke point." "Hacking incentives decay because any breach is likely to be limited in scope to a single identifier."
- **AID is NOT a domain name or UUID:** "Unlike a domain name, which is rent-seeking text, or a UUID, which is arbitrary entropy, an AID is a self-certifying identifier (SCID)."
- **SAID vs. UUID:** "The problem with a UUID is that it has no relationship to the data." A SAID is content-addressable — "An ACDC is not a mutable document; it is a crystallized fact."
- **Multisig — KERI's innovation is EXPOSING, not inventing:** in X.509/PKI multisig governance "is not knowable or verifiable by the public... This makes it impossible to distinguish between sloppy and robust management of secrets... Hackers thrive on opacity." KERI "requires a definition of the identifier's fractionally weighted thresholds directly in the public KEL." "KERI's real innovation in multisig is less about inventing and more about exposing: multisig is optional and infinitely variable, but defining and following policy about it is required."
- **JSON canonicalization / JSON-LD critique (CESR rationale):** `{"a":1,"b":2}` vs `{"b":2,"a":1}` "are semantically identical but have different cryptographic hashes." JCS "normalization logic is brittle." "In complex environments like JSON-LD, this fragility has led to 'term redefinition' vulnerabilities, where the meaning of a signed credential can be altered without invalidating the signature." (Direct swipe at W3C VC data integrity.)
- **W3C VC / JWT / X.509 = paired signatures** (a limitation) vs. ACDC anchored signatures. Foreign VCs belong as *derivative artifacts* wrapped/bridged, not as native roots of trust. **Now independently supported by a pinnable `[N]` source** — the spec's own VP comparison table, §14A.

## 10. Precise terminology & definitions

**Provenance warning (2026-09-15): the definitions below are from the untracked `.ref/` documents (§0, Warning 2) and are not pinnable.** The Dossier spec has its own tracked glossary, `spec/terms-definitions/*.md` (27 term files at `b9227753c`), which *is* pinnable; it covers the dossier-specific vocabulary (dossier, evidentum, edge, operator, threshold-operator, revocation-operator, endorsement, declination, finalization-identifier, finalizer, collector, coordinator, assembler, curator, citation, derivative, annotation-edge, predicate-edge, temporal-pinning, observation-attestation, oracle, reference-time, proximate-metadata, foreign-artifact, foreign-artifact-wrapper, bridging-party, bridge-wrapper) but **not** the KERI/ACDC/CESR base vocabulary below, which it imports by `[[tref: KERISuite, …]]`. Quotes from the tracked glossary appear inline in §1, §4, §5 and §7 above. Two glossary entries were added and one removed in this window: `declination.md` and `endorsement.md` added (`4dae85a`), `finalization-operator.md` replaced by `finalization-identifier.md` (`4dae85a`), `qualification-operator.md` deleted (`c2d261c`). **Negative result: this note never cited a qualification operator as a distinct term, so `c2d261c` supersedes nothing here.**

From primer / refs (unpinnable):
- **AID (Autonomic Identifier):** core KERI primitive, a self-certifying identifier (SCID) derived cryptographically from initial key(s). Two derivation modes:
  - *Basic derivation:* identifier = digest of a single public key; brittle, non-rotatable, "suitable only for ephemeral, short-lived use cases."
  - *Transferable derivation:* identifier derived from the entire *inception event* (initial keys + pre-rotation commitment to next keys); "The identifier string remains constant even as the keys change." KERI's primary innovation for persistent identity.
- **KEL (Key Event Log):** "the authoritative source of truth for an AID," an append-only signed chain of events; a *microledger*. Verified by replaying history from inception. Three event types: **inception (`icp`)** (birth), **rotation (`rot`)** (key/config change), **interaction (`ixn`)** (anchoring data/seals without changing keys). (KERL = the receipted KEL, implied.)
- **Establishment events:** icp + rot (define control); carry `k` (public keys) and `kt` (signing threshold).
- **Pre-rotation:** committing *now* (via hash of K2) to the next key; revealing K2 only at rotation time and re-committing to K3.
- **`kt` fractionally weighted threshold:** list(s) of fractional weights; a clause is satisfied when the sum of signing weights ≥ 1.0; lists-of-lists = AND/OR (clauses joined by OR). Example `[["1/2","1/2","1/2"], ["1/3","1/3","1/3","1/3"]]`.
- **Signature indexing (CESR):** each signature attachment carries a code specifying which key in `k` produced it, mapping signatures to weights.
- **Witness:** "a server designated by the controller to store and serve the KEL"; signs receipts; provides availability + duplicity detection. Threshold of witnesses (e.g. 2 of 3) makes an event stable. (a.k.a. backer.)
- **Watcher:** entity run by verifiers/auditors that polls witnesses to detect duplicity (conflicting head hashes).
- **SAID (Self-Addressing Identifier):** content-addressable cryptographic hash of a data structure, embedded in the `d` field via the dummy-character derivation (44 `#` chars for Base64 256-bit, digest computed then overwritten).
- **ACDC (Authentic Chained Data Container):** protocol for attestations/VCs prioritizing security, compactness, provenance; "C" = chained (any number of chains, arbitrary weight/semantics → verifiable DAG). Fixes the "copy-paste vulnerability."
- **Anchored signature:** provable KEL association (issuer digest sealed in a KEL event) vs. paired signature.
- **TEL (Transaction Event Log):** specialized log for credential status/revocation; "ACDCs anchored in TELs allow for real-time status checks without privacy leaks."
- **IPEX (Issuance and Presentation Exchange):** protocol over KERI for handing over ACDCs; `exn` messages — **`apply`** (holder requests), **`offer`** (issuer offers), **`agree`** (both agree), **`grant`** (issuer sends). (Primer §3.1 gives offer/request/grant three-step; issuance-ref §3.2 gives the four `exn` types.) Mutual anchoring: issuer logs "I issued", holder logs "I accepted" — "prevents spam (you can't force a credential into someone's wallet) and provides non-repudiation of receipt."
- **Endorsement (issuance-ref §4):** "a signature provided by an AID that is not the controller of the KEL" — external, additive, non-mutating. "You can add 100 endorsements to a single KERI event... These endorsements do not change the digest of the event." Witness receipts and watcher confirmations are forms of endorsement. This is the technical precursor to join-issuance: members endorse a *fixed artifact's SAID* rather than signing a *proposal in escrow*.
- **CESR (Composable Event Streaming Representation):** the serialization "language"; self-framing via framing-code prefix; concatenation composability (Base64 ↔ raw binary without breaking signatures); enables pipelining (route by first 1–4 bytes) and crypto agility (assign new meaning to an unused code-table prefix — old parsers still Read-Code→Lookup-Length→Read-Bytes). Sample codes: `A`=Ed25519 seed (44), `E`=Blake3-256 digest (44), `0B`=Ed25519 signature (88), `-A##`=pipeline group counter.
- **DKMI (Decentralized Key Management Infrastructure):** what the stack enables.
- **OOBI (Out-of-Band Invitation):** resolvable URL pointing to a resource serving an ACDC + KERI proofs; canonical dossier citation.

**ACDC edge operators (operators-ref — unpinnable; the ACDC specification itself is the citable source, see `raw/03-acdc-spec.md`):**
- Reserved edge field labels: `d` (SAID of edge block), `u` (UUID), `n` (SAID of target far-node ACDC), `s` (SAID of schema target must validate against), `o` (operator), `w` (weight).
- **Unary (edge-level):** `I2I` Issuer-To-Issuee (issuer of current MUST be issuee of target; *default*; standard for delegation), `NI2I` Not-Issuer-To-Issuee (may or may not), `DI2I` Delegated-Issuer-To-Issuee (issuer must be issuee or a delegated rep), `NOT` (inverts validation truthiness). "If multiple unary operators... conflict, the latest in the list takes precedence."
- **M-ary (edge-group level):** `AND` (*default*; all valid), `OR` (≥1 valid), `NAND` (not all valid), `NOR` (all invalid), `AVG` (arithmetic average of a member property), `WAVG` (weighted average using `w`).
- Doctrine: "ACDCs act as fragments of a distributed property graph. Operators allow the graph to perform 'reasoning'" — delegation chains (`I2I`), thresholds (`OR`/weighted averages), negation (`NOT` for revocation/blacklisting).

## 11. Worked examples / use-case architectural patterns (dossier §"Use Cases and Architectural Patterns", L557-615 — re-anchored 2026-09-15; the old anchor `§"Use Cases"` was incomplete)

All six are explicitly non-normative: the Scope section lists "non-normative implementation guidance through use case profiles" among what is in scope (`spec/dossier-spec-head.md` §"Scope", L131). Treat them as illustration, not requirement.

- **VVP — Compositional Dossier** (§"Verifiable Voice Protocol (VVP): The Compositional Dossier", L561-568)**:** assemble a "permission slip" from independent authorities. Distributed root of trust; verifier recursively checks issuers of edge credentials; trust from leaf authorities. Ideal for access control, licensing, regulatory compliance.
- **Law enforcement/adjudication — Procedural Dossier:** tamper-evident chain of custody + procedural evolution. Phase 1 (Investigation) = immutable "bag" of artifacts, focus on completeness/provenance; Phase 2 (Adjudication) = state changes (Marked/Offered/Admitted/Stricken) via Annotation Edges. To "strike," Clerk issues a new version with an edge targeting the original SAID + `status:"stricken"` — preserves original (for appeals) while excluding from "effective" facts.
- **Investigative journalism — Redacted Dossier:** prove provenance without revealing source. Private graph (unredacted "Source Asset") vs. public graph (Redacted Asset). Public dossier links to Redacted Asset; internally linked to Source Asset via a "blinded" edge (hash). Uses "precursor" (Cross-File Association) relationships. Lets journalist prove later (declassification) that redacted text derived from the specific original recording.
- **Mortgage qualification — Snapshot Dossier:** verify dynamic/volatile data (bank balance, credit score) via Temporal Pinning + Oracle/Observer role. "Observation Attestation" says "I observed Account X having Balance Y at Block Height Z"; dossier links the static attestation. Verify "Funds Available" at exact application moment.
- **Clinical trials — Predicate Dossier:** prove eligibility without disclosing sensitive data (HIPAA/GDPR). Zero-Knowledge Predicates via a **Predicate Edge** pointing to a ZKP/derived claim. Dossier asserts `inclusion_criteria_met: true`; evidence is a ZKP proving "Subject Age > 18 AND HIV_Status == Positive" without revealing birthdate/markers. Verifier validates the proof, not the document.
- **Petition — Open-Endorsement Dossier** (§"The Petition: The Open-Endorsement Dossier", L610-615)**:** collect a threshold of endorsements from a large/unenumerable **endorser** set (~~"signer set"~~ superseded 2026-09-15 by `b97c2f2`). Asynchronous threshold satisfaction; any AID meeting schema criteria can contribute. Uses `MxQ`: "enough qualified, unique endorsements that their weights sum to unity (with a uniform per-member weight `w`, that means at least `1/w` endorsers)" (L614); qualification proved via the `qs` schema; participants issue a qualified Endorsement ACDC and anchor it in their individual KELs. Coordinator may set `fi` and finalize when threshold reached.

## 12. Proximate metadata fields (dossier `a` section — all optional unless schema requires)

§"Standard Proximate Metadata Fields", L331-402. **Re-verified unchanged at `b9227753c` — no supersessions in this section.** All twelve fields are also carried, with matching descriptions, in `schemas/dossier-base.json` under `a.properties` (L61-127), which is the machine-readable form of the same list.

`assembly_dt` (ISO 8601 assembly time; distinct from envelope issuance date), `assembler` (AID/name of curator, useful when ≠ issuer), `purpose` (human-readable, not machine-interpreted), `ref` (external reference/case/docket/txn ID, untyped), `gov` (SAID/URI of governance framework/rulebook), `evt_dt` (ISO 8601 time of the documented event — crash/crime/filing — distinct from assembly_dt), `evt_loc` (where the event occurred; no single mandated format; SHOULD follow domain standards e.g. ISO 6709), `jur` (legal/regulatory jurisdiction as ISO 3166-1 alpha-2, optionally +3166-2; MAY be an array), `cls` (type/category of matter; domain-dependent controlled vocabulary), `phase` (procedural maturity: preliminary/factual/final, or investigation/adjudication/closed; new version SHOULD be issued on phase transition), `gov_rules` (SAID/URI of the specific ruleset governing evidence collection — more specific than `gov`; both may coexist: `gov` = who oversees, `gov_rules` = what procedural constraints applied). Also `fi` (finalization identifier — AID whose KEL carries the finalization event).

## 13. Escrow model & why joint issuance is an evolution (issuance-ref)

**Provenance warning (2026-09-15): sourced from `.ref/issuance-ref.md`, untracked and gitignored (§0, Warning 2); not pinnable, not `[N]`.** The claims about keripy's escrow machinery in particular are `[K]`-shaped claims with no `[K]` citation — they should be re-sourced against keripy at a named commit (`raw/09-keripy-code.md`) before any chapter rests on them. The final bullet's doctrine — issuance *is* anchoring — is now stated in the Dossier spec itself and can be cited there instead; see §1A.

- **Group AID:** a multisig entity whose control keys are member AIDs; the Group KEL represents the collective; every Group KEL event must be signed per the group's `kt`.
- **Wait-for-Signature (WFS) Escrow:** partial-sig events held in a "Partial Signature Escrow," "effectively 'invisible' to the authoritative state of the KEL," until weights reach threshold, then "promoted" to the KEL.
- **Core limitation — Synchronous Serialization Requirement:** "Because KERI events are chained by digests, the group must coordinate on the *content* of the event before signing." Any metadata/timestamp difference makes signatures incompatible. "This 'tight' coordination creates high friction in distributed environments." → motivates the loose, ACDC-layer, endorsement-based joint issuance where the artifact is fixed and members endorse its SAID asynchronously.
- **Issuance = anchoring/sealing** (issuance-ref §3.1): "an 'Issuance' is not just a signature on a document. It is the act of Anchoring or Sealing the credential's digest (SAID) into the Issuer's KEL." ACDC is valid only if a verifier can find that seal in a finalized establishment/interaction event.

## 14. Engineering-mindset doctrine (primer §5)

**Provenance warning (2026-09-15): sourced from `.ref/keri-primer.md`, untracked and gitignored (§0, Warning 2); not pinnable, not `[N]`.**

- Resilience via pre-rotation + weighted multisig makes identity "antifragile. It gets stronger with more keys, not more complex."
- Verification cost is O(n) in history but O(Δn) in practice via incremental verification / cached state.
- Mental-model shift: "We stop thinking about certificates (static assertions) and start thinking about event streams (dynamic histories). We stop building admin panels for identity and start building agents that manage keys. Trust is not granted by a corporation or a government, but established by the mathematical consistency of the identifier itself."
- Quantum posture: rotation authority always hidden behind a hash until used-and-discarded; hybrid governance (require both ECC and post-quantum signatures in threshold) → "The ecosystem has a straightforward, calm response to any quantum apocalypse."

## 14A. Spec front matter — scope, evidence lifespans, and the W3C VP comparison [N, newly mined 2026-09-15]

`spec/dossier-spec-head.md` was not previously a source of record for this note. It is tracked, and pinnable, and carries three things the body does not. None of this material is *new at `b9227753c`* — the VP comparison dates from `6664148` (2026-05-22) — it is new to the corpus.

**Scope, stated explicitly** (§"Scope", L129-133). In scope: "normative requirements for dossier structure and schema; joint issuance, operator semantics, and revocation; privacy-preserving and graduated disclosure; and non-normative implementation guidance through use case profiles." Out of scope, and worth quoting because it bounds what a reader may cite the spec *for*: "transport or citation protocols for presenting dossiers at transaction time; storage and hosting requirements for dossier artifacts; the internal data formats of evidence items referenced by a dossier; and the KERI, ACDC, and CESR specifications on which this document depends." So the citation *mechanism* is specified (§"Citation…") while the *protocol* that carries a citation is not — which is why the replay requirement is phrased as an obligation on the citing protocol rather than on the dossier.

**Evidence lifespans — the design premise behind anchoring** (§"Evidence Lifespans and Verification Timing", L37-51). Three classes: **temporary** ("A movie ticket, a JSON Web Token, a browser session cookie... carries authority for seconds or minutes"); **changed occasionally** ("A PIN, a password, a credit-card number, an X.509 certificate, or a magnetic key card"); and **effectively permanent** ("A birth certificate, a passport, articles of incorporation, a fingerprint or iris template, or a chain-of-custody record"). "A dossier inhabits the permanent end of this spectrum" (L45), verified "in front of audiences and against questions the issuer cannot anticipate at issuance time" — note `at issuance time`, which read `at signing time` before `b97c2f2`. Three consequences (L49-51): "The artifact must stand alone"; "Recency and freshness vary per evidentum" and are handled "through per-edge metadata and temporal pinning, not through a single global expiry"; and "State must be reconstructible at an arbitrary historical point", supported "by KERI's historical-query capability over its KELs". **This is the premise that makes §1A's "anchoring is the only mechanism that satisfies the requirement" an argument rather than an assertion.**

**Dossier vs. W3C Verifiable Presentation** (§"Relationship to W3C Verifiable Presentations", L79-99). A nine-row comparison table, of which two rows are load-bearing for the corpus. The **authentication** row: "Issuer anchors the dossier's SAID in a KEL; nothing is attached to the container itself" against, for a VP, "Holder attaches a digital signature or other proof to the presentation" (L91). The **lifecycle** row and its gloss: a VP is "created in response to a verifier's request, signed by the holder, presented once, and discarded", while a dossier is "assembled in advance, issued by the curator (not the subject) through an anchor in the curator's KEL, published at a stable location, and referenced repeatedly" (L93). The spec then names this as the single most likely reader error, which is exactly the shibboleth framing the corpus uses: "The row on authentication deserves emphasis, because it is the single easiest thing for a reader arriving from the VC world to get wrong. A dossier is not signed; nothing is attached to it" (L95). Also recorded, because it is the fair-minded half: "These differences do not put the two models in opposition. A dossier MAY reference a W3C VC as one of its evidence items through the bridging mechanism" (L99).

**Subject model, stated as a limit on the VC frame** (L97): "A VP is fundamentally a statement by a subject about themselves: 'I hold these credentials.' A dossier has no issuee." And the observation that many dossier use cases "do not have a single subject in the VC sense at all."

## 15. Bearing on `keri-doctrine.md` — a review-panel sync is owed [flagged 2026-09-15]

Recorded here rather than acted on, because synthesis is a later phase and this note does not edit chapters or the doctrine file.

**Doctrine claim 8** ("Signatures are validated against key state as of their KEL anchor, defeating the retrograde attack", `keri-doctrine.md` L43) currently rests on `was.md`, `keri-primer.md` §3.3, keripy invariant H1, and the ACDC spec §"Binding to Key State". **The Dossier spec at `b9227753c` is now a fourth, independent, `[N]`-tier source for the same claim**, and it is the only one that is a standardizing-line specification *correcting itself* toward the distinction rather than asserting it from the start — `b97c2f2`'s commit subject is literally "Distinguish anchoring from signing throughout the spec". That is evidence of a different kind: a working spec author found the conflation dangerous enough to sweep an entire document for it and to define three terms to prevent its recurrence. §1A holds the quotable sentences.

**The shibboleth row "A credential is a signed envelope (paired signature)"** (`keri-doctrine.md` L82) currently cites "ACDC spec §Binding to Key State L1673-1675; invariant H1". It could now also cite `spec/dossier-spec-body.md` §"How a Dossier Is Issued: Anchoring, Not Attached Signatures" L13-14 and `spec/dossier-spec-head.md` §"Relationship to W3C Verifiable Presentations" L95 at `b9227753c`. The Dossier citation is the better one to hand to an outsider, because it states the negative flatly ("A dossier is not signed; nothing is attached to it") and then says why in terms of key rotation rather than in terms of ACDC internals.

**Two refinements the row does not currently carry**, both from §1A, and both of which a reviewer could use against a too-absolute statement of the shibboleth: (1) the partition is drawn on **lifespan, not artifact kind** — key events and sub-minute transactional tokens *are* signed, and the spec says so; (2) an attached signature on a dossier is explicitly permitted in one narrow case (the ephemeral exception), so "ACDCs are never signed" overstates it. The accurate form is that nothing expected to outlive a key rotation is authenticated by an attached signature.

**A residual, not a correction:** `keri-doctrine.md` L19 lists "Dossier spec `3900e62`" among the pins the citations were verified against. That commit is on `schema-base-dossier`, not `main` (§0, Warning 1). The doctrine file's Dossier-derived claims were therefore verified against a feature branch. Worth re-checking which, if any, of them turn on branch-only text; the joint-issuance material did, and has since landed, but that was luck rather than method.

## 16. Open questions and gaps

1. **The `.ref` corpus has no commit and cannot acquire one without Daniel's action.** Roughly half this note's sections rest on it (§7 in part, §9, §10, §13, §14). Options, none of which this pass may take: track the three files in the Dossier repo (they are gitignored deliberately, so presumably not); re-source each claim against the tracked papers corpus at `/home/daniel/code/me/papers/` and the upstream KERI/ACDC/CESR specs, which is the standards-compliant answer and is real work; or demote the affected sections to the unmarked "spoken and first-hand report" class of standards §2 and stop citing them as evidence about design detail. **Recommend the second.** Note also that `refresh/sources.yaml` watches `.ref` for this source, where it will never fire — that is a config defect and should be corrected or removed whichever way the above goes.
2. **Was the previous pin's tier marker wrong, or was the manifest wrong?** `refresh/sources.yaml` declares one branch for this source, `main`, with role `standardizing` and marker `[N]`, and pinned it to `3900e62`, which is not on `main`. Either a mining pass followed a feature branch without saying so, or the manifest recorded a hash nobody checked. Retargeting the manifest is out of scope for a refresh (standards §6), but the next pass should not repeat it: **verify reachability from the declared branch, not just that the hash resolves.**
3. **Does anything in the corpus still describe the retired `M`/`RM`/`Q`/`FIN` model?** This note never did. `bible/05-acdc-and-verifiable-data.md` and `bible/08-presentation-architectures-and-ipex.md` draw on this note and may have picked up either model, or may have picked up `MxN` etc. from this note while carrying an `[N]` marker that was not earned until 2026-06-09. Not checkable from here — this pass owns one file. **Flagged for whoever synthesizes 05 and 08.**
4. **The intermediate `m` threshold field is a trap for anyone reading the repo's history rather than its head.** It existed on `main` for the duration of two commits on a single day (`e99878d` → `ff5b0a4`, 2026-06-09) and is not in the design. Recorded so a future pass that stumbles on it does not mine it.
5. **Whether a declination under a qualified operator must carry a qualification proof is deliberately unresolved** by the spec ("a policy choice left to the governing schema", §"Threshold Operators", L449). That is an open design point, not a gap in this note. It has a practical edge: an unqualified party's declination would then be recordable as attributable dissent under an operator whose whole premise is that only qualified parties act.
6. **`#### Slot dispositions` is the only body heading `5906e8c` left in sentence case.** Trivial, but it means the next re-anchor cannot assume title case throughout.
7. **How much of `bible/06-governance-ecosystems-and-interop.md` rests on this note is unclear.** `refresh/sources.yaml` lists chapters 05, 06 and 08 for this source, but the note's dossier-specific material maps most obviously to 05 and 08. If 06 draws on the `.ref` primer material rather than on the spec, that dependency inherits the unpinnability problem in item 1.
